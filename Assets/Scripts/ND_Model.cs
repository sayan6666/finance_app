using UnityEngine;

public class ND_Model : MonoBehaviour
{
    public static News current;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
