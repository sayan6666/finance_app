using UnityEngine;

public class CameraResolution : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Camera.main.aspect = (float)640/1136;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
